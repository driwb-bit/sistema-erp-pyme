version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app  # Esto sincroniza tu carpeta con el contenedor (útil para desarrollar)
    environment:
      - DEBUG=1